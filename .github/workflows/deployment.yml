name: Deploy
"on":
  push:
    branches:
      - main

env:
  IMAGE_NAME: ochom/sdp-sdk:latest
  CONTAINER_NAME: ${{ secrets.CONTAINER_NAME }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Docker Build
        run: docker build -t $IMAGE_NAME .

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker Push
        run: docker push $IMAGE_NAME

  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v3

  #     - id: auth
  #       name: Authenticate GCP
  #       uses: google-github-actions/auth@v0.8.0
  #       with:
  #         credentials_json: ${{ secrets.GCP_KEYS_JSON }}

  #     - name: Set up Cloud SDK
  #       uses: google-github-actions/setup-gcloud@v0.6.0

  #     - name: Restart Service
  #       run: |
  #         gcloud compute ssh --zone ${{secrets.VM_ZONE}} ${{secrets.VM_INSTANCE}}  --project ${{secrets.PROJECT_ID}} \
  #         --command "cd /var/www/deployments && \
  #         docker compose pull $CONTAINER_NAME && \
  #         docker compose up -d --build --force-recreate --remove-orphans $CONTAINER_NAME"
